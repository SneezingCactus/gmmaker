// import browser from 'webextension-polyfill';

/* browser.webRequest.onBeforeRequest.addListener(
    (req) => {
      if (req.url.includes('/js/alpha2s.js') && !req.url.includes('?')) {
        return {
          redirectUrl: browser.runtime.getURL('js/runInjectors.js'),
        };
      }
    },
    {urls: ['*://bonk.io/*']},
    ['blocking'],
);*/
